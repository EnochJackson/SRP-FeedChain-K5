name: feedchain
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  cloud_firestore: ^6.0.2
  firebase_auth: ^6.1.0
  firebase_core: ^4.1.1   # <-- update this
  google_maps_flutter: ^2.13.1
  url_launcher: ^6.3.2
  flutter_dotenv: ^6.0.0
  geolocator: ^9.0.2       # <-- compatible with Flutter 3.35/Dart 3.9
  shared_preferences: ^2.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
